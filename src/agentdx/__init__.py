"""AgentDX — deterministic replay runtime, coordination debugger and chaos harness.

The public API surface defined in PRD §8.2 (`instrument`, `@agent`, `@tool`, the
synchronisation primitives, `run`/`replay`/`analyze`, `wall_time`, `sorted_set`)
is re-exported from here. It lands with the SDK at P04; nothing is exported yet.
"""
