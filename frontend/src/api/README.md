# api/

The typed client, **generated** from the OpenAPI 3.1 schema at `/openapi.json` (CONTEXT.md §3) —
never hand-written, so a backend contract change breaks the build rather than the UI. Lands at P14.
