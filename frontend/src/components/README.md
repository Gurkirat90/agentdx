# components/

Primitives shared by the panels: Badge, Bar, SeverityDot, EvidenceLink (PRD §25). An
`EvidenceLink` always resolves to concrete event `seq` values — a finding with an empty evidence
array cannot be rendered (I6). Lands at P15.
