"""Integration tests for `agentdx.explore` against a real, Scheduler-driven harness.

`_harness.py` explains why these are hand-built scenarios shaped like `research_fanout` and
`code_pipeline` rather than the fixtures themselves.
"""
