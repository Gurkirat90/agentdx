"""Integration tests for `agentdx.runtime.faults`.

Gate G4, fault-taint causality, crash-and-retry cascade, the chaos safety suite, and the
faults-enabled determinism regression, all against a real `Scheduler`.
"""
