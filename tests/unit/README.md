# tests/unit/

Pure functions with hand-computed expectations (PRD §33.1): vector-clock operations,
happens-before, canonical serialisation, cache-key construction, virtual-clock arithmetic,
scheduler choice determinism, fault triggers, bucket classification, every verdict rule.
