"""Gate G2 / invariant I4 — never waived. See `README.md` in this directory.

Every test in this package is marked `@pytest.mark.false_positives` (registered in
`pyproject.toml`'s `[tool.pytest.ini_options]`: "healthy-fixture control (I4, gate G2)").
"""
