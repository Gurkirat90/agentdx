"""Race detector tests (`agentdx.analysis.causality`, `agentdx.analysis.race`) — P12.

Hand-authored event logs with hand-computed expected outputs (`AGENTS.md` §5), plus direct
replay of the real, committed golden fixture logs (`tests/golden/*.jsonl`) where a fixture's
own structure already exhibits the property under test. See `README.md` in this directory.
"""
