# tests/analysis/

Analyser tests over **hand-authored event logs with hand-computed expected outputs** — the payoff
of the pure-analysis design (AGENTS.md §5). Includes `test_decomposition_invariant.py`, the gate G5
check that Σ(six buckets) + residual equals the **virtual** makespan (CONTEXT.md §10 C-1).
