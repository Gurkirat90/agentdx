"""Individual benchmark harnesses (PRD §34) — one module per measurement, each runnable directly.

`bench/README.md` and each result file in `bench/results/` are the published side of what
these produce.
"""
