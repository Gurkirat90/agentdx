# research_fanout

**The healthy control — never cut, under any scope cut.** A correct fan-out/fan-in system that must
yield an empty race-findings set across all 100 determinism replays *and* the k=2 exploration
frontier (invariant I4, gate G2, never waived). A tool that reports races in correct code is worse
than no tool.
