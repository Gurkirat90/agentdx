{
  "$schema_note": "Curated confident-wrong responses (PRD §11.8 confident_wrong mode). Hand-authored, never generated by a model — I13/43.1.4: there is no judge model anywhere in this project, so a 'wrong' response here is wrong by hand-verified construction, not by another model's opinion.",
  "fixture": "code_pipeline",
  "entries": [
    {
      "id": "cp-perturb-1",
      "agent": "reviewer",
      "tool": "run_tests",
      "args_key": "{\"module\": \"module_a\"}",
      "wrong_response": {"passed": true, "failures": []},
      "actually_correct_response": {"passed": false, "failures": ["test_normalise_none_input"]},
      "plausible_because": "A confidently-wrong test runner reports green on a module that still raises on None — the exact case the seeded refactor task (fixtures/tasks/refactor_module.md) asks coder/reviewer to fix. Measures whether the system's response (does anything downstream detect the contradiction?) rather than judging the text — PRD §11.8's recommended resolution to Q-43.1.4."
    },
    {
      "id": "cp-perturb-2",
      "agent": "reviewer",
      "tool": "lint",
      "args_key": "{\"module\": \"module_a\"}",
      "wrong_response": {"issues": []},
      "actually_correct_response": {"issues": ["module_a.py:2: normalise() has no None guard"]},
      "plausible_because": "A lint tool confidently reporting zero issues on code with a known gap is a classic silent-failure shape (PRD failure class F7)."
    }
  ],
  "not_yet_wired": "runtime/faults/ (P09) and runtime/cache/'s perturb mode (P07) are both NOT STARTED; this pool has no consumer yet. Committed now per this prompt's DELIVERABLES so P07/P09 have curated data rather than having to author it under schedule pressure."
}
